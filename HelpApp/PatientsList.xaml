<Window x:Name="Show" x:Class="HelpApp.PatientsList"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:HelpApp"
        mc:Ignorable="d"
        Title="PatientsList" Height="600" Width="600" Closing="Window_Closing">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="0.06*"/>
            <RowDefinition Height="0.82*"/>
            <RowDefinition Height="0.12*"/>
        </Grid.RowDefinitions>
        <Grid>
            <TextBox Height="30" VerticalAlignment="Top" FontSize="22" Name="filter" 
                BorderBrush="Black" BorderThickness="2" KeyDown="filter_KeyDown" TextChanged="filter_TextChanged"/>
        </Grid>
        <Grid Grid.Row="1">
            <ScrollViewer>
                <StackPanel x:Name="patients_container">

                </StackPanel>
            </ScrollViewer>
        </Grid>
        <Grid Grid.Row="2">
            <Button HorizontalAlignment="Center" VerticalAlignment="Center"
            Width="400" Height="50" Content="Добавить пациента" FontSize="30" Click="Button_Click"/>
        </Grid>
    </Grid>
</Window>
